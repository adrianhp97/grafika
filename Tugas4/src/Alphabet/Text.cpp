#include "Character.h"

Character::Character(char c, int x, int y) {

}

Character::getCharacterDots(char c) {
  switch(c) {
    case (c='a'):
      return 9;
    case (c='b'):
      return
    case (c='c'):

    case (c='d'):

    case (c='e'):

    case (c='f'):

    case (c='g'):

    case (c='h'):

    case (c='i'):

    case (c='j'):

    case (c='k'):

    case (c='l'):

    case (c='m'):

    case (c='n'):

    case (c='o'):

    case (c='p'):

    case (c='q'):

    case (c='r'):

    case (c='s'):

    case (c='t'):

    case (c='u'):

    case (c='v'):

    case (c='w'):

    case (c='x'):

    case (c='y'):

    case (c='z'):


  }
}
